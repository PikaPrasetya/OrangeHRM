<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

  <Properties>
    <Property name="basePath">./logs</Property>
  </Properties>

  <Appenders>
    <!-- Console Appender -->
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="[%d{yyyy-MM-dd HH:mm:ss}] [%t] %-5level %logger{36} - %msg%n"/>
    </Console>

    <!-- Rolling File Appender -->
    <RollingFile name="File"
                 fileName="${basePath}/automation.log"
                 filePattern="${basePath}/automation-%d{yyyy-MM-dd_HH-mm}-%i.log"
                 append="true">
      <PatternLayout pattern="[%d{yyyy-MM-dd HH:mm:ss}] [%t] %-5level %logger{36} - %msg%n"/>
      <Policies>
        <OnStartupTriggeringPolicy/>
        <SizeBasedTriggeringPolicy size="10MB"/> <!-- Optional: triggers rollover when file exceeds 10MB -->
      </Policies>
      <DefaultRolloverStrategy max="7" fileIndex="min"/>
    </RollingFile>
  </Appenders>

  <Loggers>
    <Logger name="org.apache.poi.openxml4j.opc.PackageRelationshipCollection" level="info" additivity="false">
      <AppenderRef ref="File"/>
    </Logger>

    <Root level="Debug">
      <AppenderRef ref="File"/>
      <!-- <AppenderRef ref="Console"/> Uncomment if you want console output -->
    </Root>
  </Loggers>

</Configuration>
